let a = [0,3,2,1];
let b = ['a', 'd', 'c', 'b'];

const sortTwo = (a , b) => {
    for(let x = 0; x < a.length; x++){
        console.log(a[x]);
    }
    for(let j = 0; j < b.length; j++){
        console.log(b[j])
    }
}

console.log(sortTwo(a,b))